// @eventaat/shared
// Shared types, validation schemas, and constants used by api, admin, and mobile.
// Real exports will be added in later steps (e.g. User, Restaurant, Reservation types).

export const APP_NAME = "eventaat";

export type Role = "customer" | "restaurant_admin" | "platform_admin";
