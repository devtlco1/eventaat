# @eventaat/shared

Shared TypeScript types, validation schemas (zod, later), and constants used across `api`, `admin`, and `mobile`.

Currently exports only `APP_NAME` and the `Role` union as a placeholder. Real domain types (User, Restaurant, Reservation) come in later steps.
